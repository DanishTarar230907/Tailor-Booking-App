# Select "grace" Project

The FlutterFire configure command is showing your projects. Here's what to do:

## Steps:

1. **Use arrow keys** to navigate to: `grace-9bb55 (grace)`
2. **Press Enter** to select it
3. **Select platforms** - Choose at least **web** (you can use spacebar to select multiple)
4. **Press Enter** to confirm

The script will automatically generate `lib/firebase_options.dart` with your "grace" project configuration!

## Quick Reference:

- **Navigate**: Arrow keys (↑ ↓)
- **Select**: Enter
- **Select platforms**: Spacebar (for multiple), Enter to confirm

After this completes, you'll have:
- ✅ `lib/firebase_options.dart` file created
- ✅ App configured to use "grace" Firebase project
- ✅ Ready to enable Authentication and Firestore in Firebase Console

